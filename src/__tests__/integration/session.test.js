describe("Authentication", () => {
  it("should receive JWT token when authenticate with credentials", () => {
    const a = 3;
    const b = 4;

    const sum = a + b;

    expect(sum).toBe(7);
  });
});
